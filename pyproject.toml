[project]
name = "pypile"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.10.1",
    "numpy>=2.2.3",
    "plotly>=6.0.0",
]

[dependency-groups]
dev = [
    "meson>=1.7.0",
    "ninja>=1.11.1.3",
]
